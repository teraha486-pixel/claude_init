[project]
name = "dooray-mcp-server"
version = "0.1.0"
description = "AI와 연동되는 두레이 MCP 서버입니다."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.2.0",
    "httpx>=0.28.1",
    "jsonref>=1.1.0",
]
